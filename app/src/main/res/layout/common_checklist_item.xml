<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:minHeight="102dp"
    android:gravity="center_horizontal|center_vertical"
    android:background="@drawable/common_box_shadow_corners"
    android:padding="8dp"
    tools:background="@color/common_checklist_blue"
    >

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="48dp"
        android:gravity="center_horizontal"
        android:visibility="gone"
        >
         <ImageView
            android:id="@+id/common_checklist_item_label_icon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:padding="8dp"
            android:visibility="gone"
            tools:src="@drawable/common_ic_checklist_blue"
            />

    </RelativeLayout>

    <TextView
        android:id="@+id/common_checklist_item_label_name"
        android:layout_toRightOf="@+id/common_checklist_item_label_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:layout_centerVertical="true"
        android:textColor="#ffffff"
        android:textSize="@dimen/common_font_caption"
        android:textStyle="bold"
        android:ellipsize="end"
        android:maxLines="1"
        tools:text="お気に入り"
        />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="12dp"
        android:layout_marginRight="4dp"
        android:layout_marginLeft="4dp"
        android:background="#ffffff"
        />

    <include
        layout="@layout/common_checklist_item_circle_label"
        android:id="@+id/common_checklist_item_circle_label1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        />

 </LinearLayout>
