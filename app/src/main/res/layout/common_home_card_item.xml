<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:minHeight="@dimen/common_home_card_item_height"
    android:gravity="center_horizontal|center_vertical"
    android:background="@drawable/common_box_shadow_corners"
    android:padding="8dp"
    tools:background="@color/common_checklist_blue"
    >

    <TextView
        android:id="@+id/common_home_card_item_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:textSize="@dimen/common_font_subhead"
        android:textStyle="bold"
        android:ellipsize="end"
        android:maxLines="1"
        android:drawablePadding="@dimen/common_spacer_x_small"
        tools:text="お気に入り"
        />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="12dp"
        android:layout_marginRight="4dp"
        android:layout_marginLeft="4dp"
        android:background="#aaaaaa"
        />

    <TextView
        android:id="@+id/common_home_card_item_caption"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/common_font_caption"
        android:ellipsize="end"
        android:maxLines="1"
        tools:text="あ-01a ハローコトリン"
        />

 </LinearLayout>
