machine:
    services:
        - docker

dependencies:
    override:
        - docker build -t circlebinder/android-application .

test:
    override:
        - docker run circlebinder/android-application

